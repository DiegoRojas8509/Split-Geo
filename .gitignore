# Dependencies
node_modules/
frontend/node_modules/
backend/node_modules/

# Build output
frontend/dist/

# Environment variables — NEVER commit these
.env
.env.local
backend/.env
frontend/.env
# .env.production is safe to commit (no secrets, just VITE_API_URL=/api)

# OS
.DS_Store
